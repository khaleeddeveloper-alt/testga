on:
  schedule:
    - cron: "0 0 0 * *"
    - cron: "5 0 0 * *"
    
jobs:
  daily-task: 
    runs-on: ubuntu-latest
    steps:
      - name: print Message
      - run: echo "running each day each 5 minutes'
